specVersion: "v1beta"
serviceId: "local-stream"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "local-stream-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
